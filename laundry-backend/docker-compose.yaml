version: '3'
services:
  server:
    image: ${REPO_DOCKER}/${APP_NAME}:${APP_VERSION}
    build: ./
      # context: .
      # target: final
    # restart: unless-stopped
    tty: true
    ports:
      - "10001:${APP_PORT}"
    environment:
      TZ: "Asia/Jakarta"
